apiVersion: v1
kind: Service
metadata:
  name: {{ include "statuz.fullname" . }}
  labels:
    {{- include "statuz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "statuz.selectorLabels" . | nindent 4 }}
