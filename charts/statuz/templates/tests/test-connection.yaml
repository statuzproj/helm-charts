apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "statuz.fullname" . }}-test-connection"
  labels:
    {{- include "statuz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "statuz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
